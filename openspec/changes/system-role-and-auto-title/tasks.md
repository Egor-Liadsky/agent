## 1. Роль `system` в ядре (`agent-cli`)

- [x] 1.1 Добавить вариант `System` в `Role` (`crates/core/src/agent/mod.rs`)
  с serde-именем `"system"` и конструктор `Message::system`; проверить
  `cargo build -p agentcore`
- [x] 1.2 Научить `agentupstream::build_messages`
  (`crates/upstream/src/lib.rs`) склеивать `system_prompt(settings)` с
  системным сообщением истории в одно `role: "system"`, а `Role::System`
  в середине истории сериализовать как `"system"`; проверить новым тестом
  на теле запроса
- [x] 1.3 Повторить склейку в `ollama::build_messages`
  (`crates/core/src/agent/ollama.rs`); проверить тестом, что системное
  сообщение в `/api/chat` одно и идёт первым
- [x] 1.4 Добавить ветки `Role::System` в местах отображения и
  преобразования роли (`crates/cli/src/tui.rs`, `crates/cli/src/chats.rs`,
  `crates/client/src/lib.rs`, `crates/client/src/chats.rs`); проверить
  `cargo build --workspace` без предупреждений о неполных `match`
- [x] 1.5 Прогнать `cargo test` в `agent-cli` и запушить `main`

## 2. Приём ядра сервисом (`agent-sever`)

- [x] 2.1 Временно убрать `[patch]` из `.cargo/config.toml`, выполнить
  `cargo generate-lockfile` и убедиться, что
  `grep -A2 'name = "agentcore"' Cargo.lock` показывает коммит, а не путь
- [x] 2.2 Прогнать `cargo test --locked` на непропатченном ядре и вернуть
  `[patch]` для дальнейшей работы

## 3. Системное сообщение в сервисе

- [x] 3.1 Добавить в `src/config.rs` поле для `AGENTD_SYSTEM_PROMPT` с
  умолчанием сервиса; проверить тестом разбора конфигурации, что заданная
  и не заданная переменная дают ожидаемый базовый текст
- [x] 3.2 Создать модуль сборки системного сообщения (базовый текст +
  разделы стратегии, `Message::system`); покрыть юнит-тестами порядок
  разделов и случай «разделов нет»
- [x] 3.3 Перевести блок фактов в раздел системного сообщения
  (`src/facts.rs`), убрав подстановку `Message::user`; обновить тесты
  `assemble_orders_facts_then_tail_then_new_message` и соседние
- [x] 3.4 Перевести пересказ в раздел системного сообщения
  (`src/summary.rs`), убрав `SUMMARY_MARKER` из сообщения пользователя и
  комментарий про отсутствие `System`; обновить тесты сборки истории
- [x] 3.5 Добавить системное сообщение в стратегии `sliding_window`
  (`src/window.rs`) и `branching` (`src/branch.rs`); проверить тестами, что
  первое сообщение истории — роль `system`
- [x] 3.6 Научить `role_str`/`role_from_str` (`src/store.rs`) значению
  `"system"` и проверить тестом, что системные сообщения не попадают в
  `messages` и не сдвигают `seq`
- [x] 3.7 Пересчитать ожидания тестов лимита контекста
  (`estimate_tokens`/`check_context_limit` в `src/app.rs`) с учётом
  системного сообщения; `cargo test` зелёный

## 4. Автоматическое название чата

- [x] 4.1 Добавить в `src/config.rs` поля для `AGENTD_AUTO_TITLE`
  (умолчание — включено) и `AGENTD_TITLE_MODEL`; покрыть тестом разбора
  конфигурации
- [x] 4.2 Создать модуль генерации названия: промпт из системного блока и
  первого сообщения, служебные настройки вызова по образцу
  `facts::update_after_exchange`, нормализация и усечение ответа; покрыть
  юнит-тестами нормализацию (кавычки, перевод строки, длина, пустой ответ)
- [x] 4.3 Добавить в `src/store.rs` условное обновление названия
  (`UPDATE … WHERE id = ? AND title = ?`); проверить тестом, что при
  изменённом названии обновление не применяется
- [x] 4.4 Вызвать генерацию из `handle_chat_in_existing` после
  `append_exchange` через `tokio::spawn` при выполнении условий (генерация
  включена, название — умолчание, первый обмен); проверить тестом с
  `wiremock`, что название появляется в `GET /v1/chats/{id}`
- [x] 4.5 Покрыть тестами отказные ветки: ошибка провайдера не ломает
  ответ, `AGENTD_AUTO_TITLE=false` не шлёт второго запроса, чат, созданный
  со своим названием, генерацию не запускает

## 5. Документация и проверка

- [x] 5.1 Обновить README `agent-sever`: роль `system` в запросе, новые
  переменные окружения, поведение автоназвания
- [x] 5.2 Обновить README `agent-cli`: роль `system` в истории и склейка
  системного промпта провайдерами
- [x] 5.3 Прогнать `cargo test` в обоих репозиториях и вручную проверить
  сквозной сценарий (`POST /v1/chats` → `POST /v1/chat` → `GET /v1/chats`)
  с `AGENTD_DEBUG=true`: в теле запроса ровно одно системное сообщение и
  оно первое
