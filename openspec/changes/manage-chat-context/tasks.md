## 1. Поглощение черновика

- [x] 1.1 Удалить каталог `agent-sever/openspec/changes/summarize-chat-history`
      целиком и убедиться, что `openspec list` в `agent-sever` больше его не
      показывает, а годное из него перенесено в артефакты этого изменения
      (`proposal.md`, `design.md`, `specs/context-summary/spec.md`)

## 2. Часть 1: лимит контекста на сервисе (`agent-sever`, ядра не трогает)

- [x] 2.1 Написать в `src/app.rs` падающие юнит-тесты на новую семантику
      `effective_context_limit`: `(None, Some(4000))` даёт `Some(4000)`,
      `(Some(8000), Some(4000))` — `Some(4000)`, `(Some(4000), Some(8000))` —
      ошибку `context_limit_invalid`, `(None, None)` — `None`
- [x] 2.2 Изменить ветку `(None, Some(_))` в `effective_context_limit` так,
      чтобы клиентское значение действовало самостоятельно, и убедиться, что
      тесты 2.1 проходят, а прежний тест
      `effective_context_limit_rejects_client_value_without_operator_default`
      заменён новым ожиданием
- [x] 2.3 Написать падающий тест в `src/tests.rs`: `PATCH /v1/chats/{id}` с
      `settings.max_context_tokens` больше `AGENTD_MAX_CONTEXT_TOKENS`
      отвечает `400` с кодом `context_limit_invalid`, а `GET /v1/chats/{id}`
      после него показывает прежний лимит
- [x] 2.4 Перенести проверку лимита в общую часть `merge_settings`, чтобы она
      срабатывала и при `SettingsPurpose::Storage`, и не обходилась для чатов
      с провайдером `ollama`; убедиться, что тест 2.3 и тест создания чата с
      `ollama` и завышенным лимитом (`POST /v1/chats` → `400`) проходят
- [x] 2.5 Закрепить тестами в `src/tests.rs` уже работающие ветки: лимит чата
      переживает перезапись других настроек через `PATCH` без поля
      `max_context_tokens`; явный `null` в `PATCH` снимает лимит; разовое
      значение в `POST /v1/chat` не сохраняется в чате; чат с клиентским
      лимитом на сервисе без `AGENTD_MAX_CONTEXT_TOKENS` обрабатывается и
      отвечает `context_limit_exceeded` только при превышении

## 3. Часть 1: лимит контекста в консольном клиенте (`agent-cli`)

- [x] 3.1 Закрепить тестом в `crates/client/src/tests.rs`, что `POST /v1/chat`
      опускает `settings.max_context_tokens` при незаданном лимите чата и
      отправляет число при заданном (расширить существующие тесты
      комментарием о намеренной асимметрии с `ChatSettingsUpdate`)
- [x] 3.2 Закрепить тестом в `crates/client/src/chats/tests.rs`, что
      `POST /v1/chats` и `PATCH /v1/chats/{id}` отправляют поле всегда,
      включая `null` при незаданном лимите, и снабдить `ChatSettingsUpdate`
      doc-комментарием о том, почему здесь нет `skip_serializing_if`
- [x] 3.3 Закрепить тестом обратный разбор: ответ о чате с
      `settings.max_context_tokens` даёт `ChatSettings` с этим значением, а
      конструктор экрана настроек TUI показывает его в поле лимита
      (`cargo test -p agentclient -p agentcli`)

## 4. Часть 3 в ядре: поля `summary_*` в `agentcore` (`agent-cli`)

- [x] 4.1 Добавить в `ChatSettings` (`crates/core/src/config.rs`) поля
      `summary_enabled: Option<bool>`, `summary_keep_messages: Option<u32>`,
      `summary_step_messages: Option<u32>` с `#[serde(default)]` и
      doc-комментариями о том, что `None` — операторское умолчание сервиса;
      тест: старый JSON настроек без этих полей разбирается, поля `None`
- [x] 4.2 Добавить те же умолчания для новых чатов в `Config` и перенос их в
      `default_chat_settings`; тест: изменение умолчания в конфиге не влияет
      на уже созданный чат (по образцу теста `max_context_tokens`)
- [x] 4.3 Прогнать `cargo test` в `agent-cli` и убедиться, что ядро
      собирается без терминальных зависимостей

## 5. Часть 3 в клиенте и TUI (`agent-cli`)

- [x] 5.1 Передавать `summary_*` из `ChatSettings` в payload `POST /v1/chat`
      (`crates/client/src/lib.rs`) с `skip_serializing_if = "Option::is_none"`
      — теми же правилами, что у `max_context_tokens`; тест на присутствие и
      отсутствие полей в теле
- [x] 5.2 Передавать `summary_*` в `ChatSettingsUpdate`
      (`crates/client/src/chats.rs`) всегда, включая `null`; тест на тело
      `POST /v1/chats` и `PATCH /v1/chats/{id}`
- [x] 5.3 Добавить поля компактизации в экран настроек чата
      (`crates/cli/src/tui.rs`) рядом с полем лимита контекста: чтение из
      `ChatSettings`, разбор введённых значений с ошибкой на нечисловом и
      нулевом, сбор обратно в `ChatSettings` при сохранении; правки точечные
- [x] 5.4 Показать умолчания компактизации в `config show` и добавить команды
      их настройки (`crates/cli/src/main.rs`) по образцу
      `config context-limit`; проверить `cargo run -p agentcli -- config show`
- [x] 5.5 Прогнать `cargo test` в `agent-cli` целиком и
      `cargo clippy --all-targets`

## 6. Пуш ядра и пересборка зависимости

- [x] 6.1 Закоммитить и запушить `agent-cli` в `main` (сообщение коммита — на
      английском, в повелительном наклонении)
- [x] 6.2 В `agent-sever` временно убрать `[patch]` из
      `.cargo/config.toml`, выполнить `cargo generate-lockfile` и проверить,
      что в лок-файле коммит, а не путь:
      `grep -A2 'name = "agentcore"' Cargo.lock`; затем прогнать
      `cargo test --locked` и вернуть `[patch]` на место (файл не коммитить)

## 7. Часть 2: конфигурация суммаризации (`agent-sever`)

- [x] 7.1 Добавить в `Config` (`src/config.rs`) поля `summary_enabled: bool`,
      `summary_keep_messages: u32`, `summary_step_messages: u32`,
      `summary_max_chars: u32`, `summary_model: Option<String>` из переменных
      `AGENTD_SUMMARY_ENABLED` (`false`), `AGENTD_SUMMARY_KEEP_MESSAGES` (20),
      `AGENTD_SUMMARY_STEP_MESSAGES` (10), `AGENTD_SUMMARY_MAX_CHARS`,
      `AGENTD_SUMMARY_MODEL`, числовые — существующим
      `parse_optional_positive`-подходом
- [x] 7.2 Покрыть тестами в `src/config.rs`: умолчания при пустом окружении,
      применение заданных значений, фатальная ошибка на нечисловом и нулевом
      значении каждой числовой переменной (`cargo test config`)

## 8. Часть 2: хранение пересказа (`agent-sever`)

- [x] 8.1 Написать `migrations/0002_chat_summaries.sql` (таблица
      `chat_summaries` с `chat_id TEXT PRIMARY KEY REFERENCES chats (id) ON
      DELETE CASCADE`, `summary`, `through_seq`, `updated_at`) и проверить
      тестом, что миграция применяется при открытии пула на временном файле
- [x] 8.2 Добавить в `src/store.rs` `ChatSummary`, `load_summary` и
      `save_summary` (`INSERT ... ON CONFLICT (chat_id) DO UPDATE`, не двигая
      `through_seq` назад); тесты: сохранение, перезапись, чтение
      отсутствующего пересказа даёт `None`
- [x] 8.3 Проверить тестами, что удаление чата удаляет его пересказ каскадом,
      а `load_messages` после сохранения пересказа по-прежнему отдаёт все
      сообщения

## 9. Часть 2: сборка компактизованной истории (`agent-sever`)

- [x] 9.1 Реализовать в `src/app.rs` чистую функцию выбора границы: по
      сохранённым сообщениям, `keep_messages` и `through_seq` вернуть хвост
      (последние `N`, граница сдвинута вперёд до ближайшего сообщения роли
      `user` — решение 5 в `design.md`) и список вытесняемых, ещё не
      пересказанных сообщений; юнит-тесты: чат короче `N`, чат длиннее `N`,
      граница попадает на `assistant`
- [x] 9.2 Реализовать подстановку пересказа первым сообщением роли `user` с
      маркером-заголовком; юнит-тест на состав итоговой истории (пересказ,
      хвост, новое сообщение)
- [x] 9.3 Реализовать правило ступени (пересказ перестраивается только при
      накоплении не меньше шага не пересказанных вытесненных сообщений);
      юнит-тесты на пороге, ниже и выше порога
- [x] 9.4 Реализовать построение пересказа: настройки чата с моделью из
      `summary_model`, выключенными рассуждением и `thinking`, обычным
      форматом ответа; вызов `Agent::ask` одним сообщением из прежнего
      пересказа, вытесняемых сообщений и инструкции уложиться в
      `summary_max_chars`; результат усекается до `summary_max_chars`
      (юнит-тест на усечение)
- [x] 9.5 Встроить компактизацию в `handle_chat_in_existing` до
      `check_context_limit`: на ступени — построить, сохранить и использовать;
      иначе — использовать сохранённый пересказ
- [x] 9.6 Реализовать деградацию при ошибке вызова: `WARN` без текста
      пересказа (текст — только при `AGENTD_LOG_CONTENT=true`), прежние
      пересказ и `through_seq` не меняются, история собирается из прежнего
      пересказа, а при его отсутствии — полностью

## 10. Часть 3 на сервисе: настройки компактизации в контракте (`agent-sever`)

- [x] 10.1 Добавить в `ChatSettingsDto` (`src/dto.rs`) поля `summary_enabled`,
      `summary_keep_messages`, `summary_step_messages` через `double_option` и
      наложение в `apply_to` по образцу `max_context_tokens`; юнит-тесты:
      поля нет — сохранённое значение, `null` — сброс к умолчанию, число —
      задаёт
- [x] 10.2 Добавить код ошибки `summary_settings_invalid` в `src/error.rs` и
      проверку границ (клиентский `summary_keep_messages` не больше
      операторского, `summary_step_messages` не меньше операторского, нулевые
      значения отвергаются) в общую часть `merge_settings`, чтобы она
      действовала и при сохранении, и при разовом переопределении
- [x] 10.3 Тесты в `src/tests.rs`: завышенный `summary_keep_messages` и
      заниженный `summary_step_messages` дают `400 summary_settings_invalid`
      и в `PATCH /v1/chats/{id}`, и в `POST /v1/chat`, а настройки чата не
      меняются
- [x] 10.4 Тест: чат с `summary_enabled: true` компактизуется на сервисе с
      `AGENTD_SUMMARY_ENABLED=false`, а чат без своего значения — нет

## 11. Часть 2: наблюдаемость (`agent-sever`)

- [x] 11.1 Добавить в ответ `POST /v1/chat` блок `context` (число заменённых
      пересказом сообщений, признак построения пересказа на этом запросе);
      тест на состав ответа для компактизованного и некомпактизованного
      запроса
- [x] 11.2 Добавить в DTO ответа о чате поля `summary: Option<String>` и
      `summary_through_seq: Option<i64>` для `GET /v1/chats/{id}` и
      `GET /v1/chats`; тесты: `null` для чата без пересказа, значения — для
      чата с пересказом, `400` на попытку прислать эти поля в `PATCH`
- [x] 11.3 Добавить запись в журнал о построении пересказа и числе заменённых
      сообщений независимо от `AGENTD_LOG_CONTENT` (сам текст — только при
      нём); проверить вручную по выводу сервиса

## 12. Интеграционные тесты сервиса через `wiremock` (`agent-sever`)

- [x] 12.1 При выключенной компактизации длинный чат уходит провайдеру целиком
      и обращений за пересказом нет
- [x] 12.2 При включённой компактизации и чате длиннее `N` на ступени
      делается ровно два обращения к провайдеру (пересказ и ответ), тело
      второго содержит пересказ первым сообщением и не содержит вытесненных
      сообщений, пересказ сохранён в БД
- [x] 12.3 Следующий запрос до новой ступени делает одно обращение и
      подставляет сохранённый пересказ
- [x] 12.4 Ошибка провайдера на вызове пересказа не ломает запрос:
      пользователь получает ответ, сохранённый пересказ не изменён
- [x] 12.5 Чат, чья полная история превысила бы эффективный лимит, после
      компактизации обрабатывается без `context_limit_exceeded`, а если и
      компактизованная история превышает лимит — `400 context_limit_exceeded`
      без вызова по пользовательскому сообщению
- [x] 12.6 `POST /v1/chat` без `chat_id` при включённой компактизации шлёт
      присланную историю целиком и не делает дополнительного обращения к
      провайдеру

## 13. Документация

- [x] 13.1 Обновить README `agent-sever`: пять переменных `AGENTD_SUMMARY_*`,
      поведение компактизации (дословный хвост, ступень, подстановка,
      деградация), поля `settings.summary_*` и границы клиентских значений,
      новые поля ответов, изменившаяся семантика `max_context_tokens`
      (клиентский лимит без операторского и проверка при сохранении)
- [x] 13.2 Обновить README `agent-cli`: поля `summary_*` в настройках чата,
      умолчания в конфиге и `config show`, горячие клавиши и поля экрана
      настроек чата TUI, поведение отправки полей в двух видах запросов

## 14. Финальная проверка

- [x] 14.1 `cargo test` и `cargo clippy --all-targets` в `agent-cli` — без
      предупреждений
- [x] 14.2 `cargo test --locked` и `cargo clippy --all-targets` в
      `agent-sever` без `[patch]` — без предупреждений; после проверки
      вернуть `[patch]`
- [x] 14.3 Ручная проверка: поднять сервис с `AGENTD_SUMMARY_ENABLED=true`,
      малыми `AGENTD_SUMMARY_KEEP_MESSAGES` и `AGENTD_SUMMARY_STEP_MESSAGES`,
      провести чат из TUI через две ступени и убедиться, что
      `summary_through_seq` растёт, блок `context` в ответе показывает число
      заменённых сообщений, а `GET /v1/chats/{id}/messages` отдаёт полную
      переписку
