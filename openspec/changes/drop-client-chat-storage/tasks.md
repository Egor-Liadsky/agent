## 1. Сервис: эндпоинт записи готового обмена (`agent-sever`)

- [ ] 1.1 Добавить в `src/dto.rs` тело запроса записи обмена (`prompt`,
  `reply` с `content`, `reasoning`, `model`, `usage`, `timing`) и ответ
  (`chat_id`, оба сохранённых сообщения в виде `MessageView`); проверка —
  `cargo test` в `agent-sever` собирается и существующие тесты проходят
- [ ] 1.2 Добавить в `src/app.rs` маршрут и обработчик
  `POST /v1/chats/{id}/messages`: проверка владельца, валидация пустых
  `prompt` и `reply.content`, отказ на теле с `messages`, вызов существующей
  `store::append_exchange`, ответ `201`; проверка — обработчик доступен в
  роутере и `cargo test` проходит
- [ ] 1.3 Убедиться, что имя модели из `reply.model` сохраняется без проверки
  по `AGENTD_ALLOWED_MODELS` и что модель не вызывается; проверка — тест из
  задачи 1.4 со сторонним именем модели
- [ ] 1.4 Написать тесты эндпоинта в `src/tests.rs`: успешная запись с
  продолжением нумерации `seq` и обновлением `updated_at`, сохранение
  телеметрии, модель вне белого списка, пустой `prompt`, отсутствующий
  `reply.content`, тело с `messages`, запись в чужой и несуществующий чат
  (`404` `chat_not_found`), запись без токена при включённой аутентификации;
  проверка — `cargo test` в `agent-sever` проходит целиком
- [ ] 1.5 Проверить, что тексты запроса и ответа попадают в журнал только при
  `AGENTD_LOG_CONTENT=true`; проверка — тест или ручной прогон с обоими
  значениями переменной

## 2. Клиент чатов в `agentclient` (`agent-cli`)

- [ ] 2.1 Добавить в `crates/client` типы представления чата и сообщения
  (`id`, `title`, `settings`, `created_at`, `updated_at`, `message_count`;
  `seq`, `role`, `content`, `created_at`, `reasoning`, `model`, `usage`,
  `timing`) и разбор ответов контракта `/v1`; проверка — `cargo test` в
  `agent-cli` собирается
- [ ] 2.2 Реализовать операции над чатами: создание, список с `limit`/`cursor`
  и возвратом `next_cursor`, чтение с `limit`/`after` и возвратом
  `next_after`, переименование и изменение настроек, удаление; проверка —
  тесты из задачи 2.6
- [ ] 2.3 Реализовать отправку сообщения в чат: `POST /v1/chat` с `chat_id` и
  `prompt` без поля `messages`, с возвратом `chat_id` и `seq`; проверка —
  тест на `wiremock` сверяет тело запроса
- [ ] 2.4 Реализовать запись готового обмена
  (`POST /v1/chats/{id}/messages`) с передачей рассуждения, имени модели и
  телеметрии; проверка — тест на `wiremock` сверяет тело запроса
- [ ] 2.5 Ввести типизированную ошибку операций над чатами с отдельным
  вариантом «чат не найден» для `404` `chat_not_found`, остальные коды —
  через существующий `parse_service_error` с сохранением `request_id`;
  проверка — тесты из задачи 2.6 различают причины по типу, а не по тексту
- [ ] 2.6 Написать тесты клиента чатов в `crates/client/src/tests.rs` на
  `wiremock`: обход страниц списка и истории, отправка сообщения в чат,
  запись обмена, `401`, `404` `chat_not_found`, `400`, ошибка с `request_id`
  только в заголовке `x-request-id`, недоступный сервис; проверка —
  `cargo test` в `agent-cli` проходит

## 3. Клиент: отказ от локального хранилища (`agent-cli`)

- [ ] 3.1 Убрать из `crates/cli/src/chats.rs` `ChatSession`, `save_chat`,
  `list_chats`, `delete_chat` и `context_block`, оставив
  `last_activity_label`; проверка — в модуле не осталось чтения и записи
  файлов чатов, `cargo build` проходит после задач раздела 4
- [ ] 3.2 Добавить однократное удаление устаревшего каталога чатов при старте
  клиента с предупреждением, содержащим путь и указание на безвозвратную
  потерю переписки; удалять только каталог `chats` внутри директории
  конфигурации, неудачу удаления показывать сообщением и продолжать работу;
  проверка — тест на временной директории и ручной прогон на каталоге с
  файлами
- [ ] 3.3 Ввести в TUI тип состояния чата (`id`, `title`, `settings`,
  загруженные сообщения, `message_count`, признак черновика, `updated_at`)
  вместо `ChatSession`; проверка — `cargo build` в `agent-cli` проходит

## 4. TUI: список, история и операции с сервера (`agent-cli`)

- [ ] 4.1 Загружать список чатов с сервера страницами и догружать следующую
  страницу по `next_cursor` при прокрутке к концу списка, без дублирования
  уже показанных чатов; проверка — ручной прогон на сервисе с числом чатов
  больше страницы
- [ ] 4.2 Загружать переписку выбранного чата страницами до `next_after ==
  null`, показывая состояние ожидания вместо пустого чата; проверка — ручной
  прогон на чате длиннее одной страницы
- [ ] 4.3 Сделать новый чат черновиком в памяти и создавать его на сервере
  (`POST /v1/chats` с накопленными настройками) при первой отправке, следом
  отправляя `POST /v1/chat { chat_id, prompt }`; проверка — открытие и выход
  без отправки не меняет `GET /v1/chats`
- [ ] 4.4 Отправлять переименование, изменение настроек (`Ctrl+P`) и удаление
  чата на сервер (`PATCH` и `DELETE /v1/chats/{id}`), а для черновика —
  применять их к телу создания; проверка — ручной прогон каждой операции с
  проверкой через `GET /v1/chats`
- [ ] 4.5 После первого успешного обмена переименовывать чат первыми 40
  символами первого сообщения через `PATCH`, только если заголовок всё ещё
  `Новый чат`; проверка — ручной прогон с автоматическим и вручную заданным
  заголовком
- [ ] 4.6 Собирать блок переноса контекста из `GET /v1/chats/{id}`,
  прочитанного страницами до конца, сохранив прежнюю форму блока; проверка —
  перенос чата длиннее одной страницы содержит все сообщения по порядку
- [ ] 4.7 Добавить состояние ошибки панели вместо списка или переписки:
  человекочитаемая причина по типу ошибки, `request_id`, если он есть, и
  способ повторить запрос; введённый текст сообщения при отказе не теряется;
  проверка — прогон с остановленным сервисом и с неверным `client_token`
- [ ] 4.8 Выполнять новые сетевые операции через существующую схему
  `tokio::spawn` и `ChatEvent`, не блокируя ввод; проверка — во время
  загрузки списка и истории TUI отвечает на клавиши и показывает спиннер

## 5. Чаты локального Ollama (`agent-cli`)

- [ ] 5.1 После успешного локального ответа `OllamaAgent` записывать обмен в
  чат через `POST /v1/chats/{id}/messages` вместе с рассуждением, именем
  модели и телеметрией; проверка — обмен виден в `GET /v1/chats/{id}` с той
  же телеметрией, что и в клиенте
- [ ] 5.2 Показывать явную ошибку, если локальный ответ получен, а запись
  обмена не удалась, не повторяя запись автоматически; проверка — прогон с
  остановленным `agentd` и работающим Ollama
- [ ] 5.3 Убедиться, что чат Ollama создаётся на сервере тем же путём, что и
  облачный, и попадает в общий список; проверка — ручной прогон нового чата с
  провайдером `ollama`

## 6. Разовые вызовы и общая проверка (`agent-cli`)

- [ ] 6.1 Убедиться, что `agentcli ask` шлёт `POST /v1/chat` без `chat_id`,
  не создаёт чатов на сервере и не создаёт файлов у клиента; проверка —
  прогон команды со сверкой `GET /v1/chats` и содержимого директории
  конфигурации
- [ ] 6.2 Прогнать `cargo test` в `agent-cli` и `cargo build --release`;
  проверка — обе команды завершаются успешно
- [ ] 6.3 Прогнать `cargo test` в `agent-sever`; проверка — тесты проходят.
  Пуш `agent-cli` в `main` и обновление `Cargo.lock` не выполняются:
  `agentcore` и `agentupstream` не менялись — сверить командой
  `git status` в `agent-cli`, что изменения только в `crates/cli` и
  `crates/client`

## 7. Документация

- [ ] 7.1 Обновить `agent-cli/README.md`: чаты хранятся только на сервере,
  удаление старого каталога чатов при обновлении, поведение при недоступном
  сервисе, смена `client_token` как смена владельца чатов, путь чатов Ollama,
  горячие клавиши и команды; проверка — README не описывает
  `~/.config/agentcli/chats/` как хранилище
- [ ] 7.2 Обновить `agent-sever/README.md`: описание
  `POST /v1/chats/{id}/messages` в контракте `/v1`, его тело, коды ошибок и
  указание, что модель при этом не вызывается; проверка — эндпоинт есть в
  списке эндпоинтов README
