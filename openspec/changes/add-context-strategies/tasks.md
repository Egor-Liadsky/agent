## 1. Ядро `agent-cli`: типы настроек

- [x] 1.1 Добавить в `crates/core/src/config.rs` перечисление `ContextStrategy`
  (`summary`, `sliding_window`, `facts`, `branching`, `snake_case` в serde) и
  поля `context_strategy: Option<ContextStrategy>`, `context_window_messages:
  Option<u32>` в `ChatSettings`; проверить `cargo test -p agentcore`
- [x] 1.2 Добавить те же умолчания в `Config` и их перенос в `ChatSettings`
  нового чата; проверить тестом, что заданное в конфиге умолчание попадает в
  новый чат, а изменение конфига не переписывает настройки существующего чата
- [x] 1.3 Добавить тесты совместимости: старый `config.toml` и старый JSON
  чата без новых полей читаются в `None` без ошибки разбора
- [x] 1.4 Прогнать `cargo test` в `agent-cli` целиком

## 2. Ядро `agent-cli`: клиент HTTP

- [x] 2.1 Провести новые поля настроек через DTO `crates/client/src/chats.rs`
  (создание, правка, разовое переопределение); проверить тестами крейта, что
  поля сериализуются и что незаданное поле не отправляется
- [x] 2.2 Добавить в `agentclient` вызовы фактов (`GET` списка, `PUT` ключа,
  `DELETE` ключа) и веток (список, создание, активация, чтение истории ветки);
  покрыть тестами против поддельного сервера
- [x] 2.3 Добавить разбор расширенного блока `context` ответа (`strategy`,
  отброшенные сообщения, число фактов, ветка); проверить тестом разбора
- [x] 2.4 Прогнать `cargo test` в `agent-cli` и запушить `agent-cli` в `main`
  (сервису нужны новые поля `agentcore` из `main`)

## 3. Сервис: схема и конфигурация

- [x] 3.1 Написать миграцию `migrations/0003_context_strategies.sql`: таблицы
  `chat_facts` и `chat_branches`, колонки `chats.active_branch` и
  `messages.branch_id`, замена `UNIQUE (chat_id, seq)` на
  `UNIQUE (branch_id, seq)`, создание корневой ветки каждому чату и перенос
  существующих сообщений в неё
- [x] 3.2 Добавить тест миграции: БД, наполненная по старой схеме, после
  применения миграций отдаёт все сообщения чата и ровно одну корневую
  активную ветку
- [x] 3.3 Добавить в `src/config.rs` переменные `AGENTD_CONTEXT_STRATEGY`,
  `AGENTD_ALLOWED_CONTEXT_STRATEGIES`, `AGENTD_CONTEXT_WINDOW_MESSAGES`,
  `AGENTD_MAX_FACTS`, `AGENTD_FACT_VALUE_MAX_CHARS`, `AGENTD_FACTS_MODEL`,
  `AGENTD_MAX_BRANCH_DEPTH`; проверить тестами разбор значений и фатальный
  отказ старта при неизвестном имени стратегии
- [x] 3.4 Добавить коды ошибок `context_strategy_invalid`,
  `context_strategy_not_allowed`, `context_window_invalid`,
  `facts_limit_exceeded` в `src/error.rs`; проверить тестом конверт ответа

## 4. Сервис: переключатель стратегий

- [x] 4.1 Ввести в сервисе трейт сборки истории и структуру результата
  (`history` + наблюдаемость), обернуть существующую компактизацию
  `src/summary.rs` как стратегию `summary`; проверить, что имеющиеся тесты
  компактизации проходят без изменения ожиданий
- [x] 4.2 Заменить в `handle_chat_in_existing` прямой вызов `compact_history`
  выбором стратегии по действующим настройкам; проверить тестом, что при
  стратегии, отличной от `summary`, поддельный провайдер получает ровно один
  вызов
- [x] 4.3 Провести валидацию стратегии на путях сохранения (`POST /v1/chats`,
  `PATCH /v1/chats/{id}`) и вызова (`POST /v1/chat`); проверить тестами `400`
  с кодами `context_strategy_invalid` и `context_strategy_not_allowed`
- [x] 4.4 Расширить блок `context` в `src/dto.rs` полем `strategy` и
  счётчиками сборки, опуская поля чужих стратегий; проверить тестами формы
  ответа для каждой стратегии
- [x] 4.5 Считать лимит контекста по собранной стратегией истории; проверить
  тестом, что окно спасает чат, полная история которого лимит превышает

## 5. Сервис: стратегия Sliding Window

- [x] 5.1 Реализовать `src/window.rs`: последние N сообщений, сдвиг границы к
  сообщению пользователя; покрыть модульными тестами границы (история короче
  окна, граница на ответе модели, ровно на границе)
- [x] 5.2 Проверить границы настройки `context_window_messages` относительно
  операторского потолка; тесты на `400 context_window_invalid` при нуле и при
  превышении потолка
- [x] 5.3 Тест сквозного запроса: при 20 сохранённых сообщениях и окне 6
  поддельный провайдер получает 6 сообщений и новое, а чтение чата
  по-прежнему возвращает все 20

## 6. Сервис: стратегия Sticky Facts

- [x] 6.1 Реализовать хранение фактов в `src/store.rs` (чтение набора,
  установка ключа, удаление ключа, удаление вместе с чатом); покрыть тестами
  хранилища
- [x] 6.2 Реализовать `src/facts.rs`: промпт обновления, разбор ответа как
  списка операций `set`/`delete`, применение к набору, усечение по потолкам;
  модульные тесты на неразбираемый ответ, удаление ключа, превышение потолка
- [x] 6.3 Встроить обновление фактов после записи обмена; тест: ошибка
  провайдера на обновлении фактов не меняет основной ответ, факты остаются
  прежними, а блок `context` сообщает, что факты не обновлены
- [x] 6.4 Реализовать сборку истории «блок фактов + последние N сообщений +
  новое сообщение», без пустого блока при пустом наборе; тест порядка
  сообщений в запросе к поддельному провайдеру
- [x] 6.5 Добавить эндпоинты `GET /v1/chats/{id}/facts`,
  `PUT /v1/chats/{id}/facts/{key}`, `DELETE /v1/chats/{id}/facts/{key}` с
  проверкой владельца; тесты на правку, `404` на отсутствующий ключ, ответ
  чужому владельцу и `400 facts_limit_exceeded`

## 7. Сервис: стратегия Branching

- [x] 7.1 Реализовать работу с ветками в `src/store.rs`: создание ветки от
  сообщения, список веток с числом сообщений, переключение активной, запись
  сообщений в активную ветку; тесты хранилища
- [x] 7.2 Реализовать `src/branch.rs`: сборка истории подъёмом по цепочке
  родителей с учётом `fork_seq` и потолка `AGENTD_MAX_BRANCH_DEPTH`;
  модульные тесты на две ветки от одной точки и на вложенную ветку
- [x] 7.3 Добавить эндпоинты `GET /v1/chats/{id}/branches`,
  `POST /v1/chats/{id}/branches`,
  `POST /v1/chats/{id}/branches/{branch_id}/activate`; тесты на `404` при
  ветвлении от несуществующего сообщения и при обращении к чужому чату
- [x] 7.4 Дать чтению чата параметр ветки и вернуть в ответе идентификатор
  ветки, чья история отдана; тест чтения неактивной ветки без смены активной
- [x] 7.5 Сквозной тест независимости веток: продолжение диалога в одной ветке
  не попадает в запрос другой

## 8. Клиент: TUI и подкоманды

- [x] 8.1 Добавить в форму настроек чата `crates/cli/src/tui.rs` поле выбора
  стратегии и поле размера окна; проверить запуском `cargo run -p agentcli --
  chat`, что переключение сохраняется в чате
- [x] 8.2 Показать в интерфейсе чата действующую стратегию и данные блока
  `context` (отброшенные сообщения, обновление фактов, ветка)
- [x] 8.3 Добавить экран фактов: список, правка значения, удаление ключа
- [x] 8.4 Добавить экран веток: список с пометкой активной, создание ветки от
  выбранного сообщения, переключение; проверить, что после переключения окно
  чата показывает историю выбранной ветки
- [x] 8.5 Добавить подкоманды в `crates/cli/src/cli.rs` и `chats.rs` для
  фактов и веток без TUI; проверить их выводом команд

## 9. Сравнение стратегий

- [x] 9.1 Положить сценарий сбора ТЗ (10–15 сообщений пользователя плюс
  контрольные вопросы о деталях из начала) в
  `agent-sever/tests/data/context-scenario.json`
- [x] 9.2 Добавить в `src/tests.rs` прогон сценария против `wiremock` по всем
  четырём стратегиям: собрать по каждой число отправленных сообщений,
  оценку токенов (включая вспомогательные вызовы) и ответы на контрольные
  вопросы; для `branching` — checkpoint, две ветки и переключение между ними
- [x] 9.3 Прогнать сценарий против живой модели одинаковыми настройками для
  всех стратегий и сохранить сырые числа прогона
- [x] 9.4 Написать `agent-sever/docs/context-strategies-comparison.md`:
  таблица по четырём осям (качество, устойчивость, расход токенов, удобство),
  модель, дата, параметры, команда прогона и рекомендация по применимости

## 10. Документация и сквозная проверка

- [x] 10.1 Обновить `agent-cli/README.md`: стратегии контекста, новые
  настройки чата, экраны фактов и веток, горячие клавиши, подкоманды
- [x] 10.2 Обновить `agent-sever/README.md`: новые переменные окружения,
  эндпоинты фактов и веток, поведение каждой стратегии, блок `context`, цена
  отката миграции `0003`
- [x] 10.3 Прогнать `cargo test` в `agent-cli`, затем в `agent-sever` убрать
  `[patch]` из `.cargo/config.toml`, выполнить `cargo generate-lockfile` и
  прогнать `cargo test --locked`; проверить
  `grep -A2 'name = "agentcore"' agent-sever/Cargo.lock`, что зафиксирован
  коммит, а не путь, затем вернуть `[patch]`
- [x] 10.4 Проверить вручную сквозной сценарий: поднять `agentd`, из TUI
  создать чат, пройти по каждой стратегии несколько сообщений, создать две
  ветки от одной точки и переключиться между ними
