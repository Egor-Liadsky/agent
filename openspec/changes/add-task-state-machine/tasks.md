## 1. Ядро: настройки чата (`agent-cli`)

- [x] 1.1 Добавить в `agentcore::config::ChatSettings` поля
  `task_state_enabled: Option<bool>` и `task_state_auto_enabled:
  Option<bool>` с `#[serde(default)]` и русскими doc-комментариями про
  операторские умолчания; проверка — `cargo test` в `agent-cli` проходит, а
  десериализация старого файла чата без этих полей даёт `None`
  (тест на сериализацию/десериализацию `ChatSettings`).
- [x] 1.2 Прогнать `cargo test` в `agent-cli` и убедиться, что терминальные
  крейты в ядро не утекли (`cargo tree -p agentcore | grep -E
  'ratatui|crossterm|clap'` пуст).

## 2. Ядро: пуш и фиксация зависимости

- [x] 2.1 Запушить `agent-cli` в `main` (коммит правки `ChatSettings`);
  проверка — `git log origin/main -1` в `agent-cli` показывает этот коммит.
- [x] 2.2 В `agent-sever` временно снять `[patch]` из
  `.cargo/config.toml`, выполнить `cargo generate-lockfile` и прогнать
  `cargo test --locked`; проверка —
  `grep -A2 'name = "agentcore"' agent-sever/Cargo.lock` показывает
  коммит, а не путь.

## 3. Сервис: хранилище и миграция (`agent-sever`)

- [x] 3.1 Написать `migrations/0006_task_state.sql`: таблицы `chat_tasks`
  (`id`, `chat_id`, `stage`, `step`, `expected_action`, `paused`,
  `resume_brief`, `created_at`, `updated_at`) и `chat_task_transitions`
  (`id`, `task_id`, `from_stage`, `to_stage`, `source`, `reason`,
  `created_at`) с индексами по `chat_id` и `task_id`; проверка — сервис
  стартует на пустой базе и `cargo test` в `agent-sever` проходит.
- [x] 3.2 Реализовать в `src/store.rs` ленивое создание строки задачи
  (`planning`, пустые тексты), чтение состояния, запись перехода и
  обновление шага и ожидаемого действия; проверка — тест: чтение состояния
  чата, созданного до миграции, возвращает `planning` и не возвращает
  ошибку.
- [x] 3.3 Провести завершение задачи через общий путь: `store::finish_task`
  вызывается переходом в `done` в одной транзакции с записью перехода, чат
  получает новую задачу в `planning`; проверка — тест: после перехода в
  `done` рабочая память пуста, `active_task_id` изменился, журнал новой
  задачи пуст, а отмеченные ключи оказались в долговременной памяти.

## 4. Сервис: автомат и состояние задачи

- [x] 4.1 Создать `src/task.rs` с `TaskStage`, разбором и сериализацией
  имён этапов и `can_transition` по списку рёбер из design.md (решение 3);
  проверка — юнит-тесты на все допустимые рёбра и на отказ для пропуска
  этапа, перехода из `done` и неизвестного имени этапа.
- [x] 4.2 Реализовать применение перехода с текстами шага и ожидаемого
  действия и операторскими лимитами длины; проверка — тест: текст сверх
  лимита даёт ошибку и не меняет ни этап, ни шаг (частичного применения
  нет).
- [x] 4.3 Реализовать паузу, возобновление и запрет изменений на паузе;
  проверка — тесты: пауза сохраняет этап, шаг и ожидаемое действие; переход
  на паузе отклоняется; повторная пауза и повторное возобновление
  идемпотентны.
- [x] 4.4 Реализовать сборку брифа возобновления из состояния и рабочей
  памяти с обрезкой по операторскому лимиту; проверка — тесты: бриф
  содержит этап, шаг, ожидаемое действие и ключи рабочей памяти; при паузе
  к провайдеру не уходит ни одного запроса (`wiremock` без ожидаемых
  вызовов); повторная пауза перезаписывает бриф новым шагом.

## 5. Сервис: конфигурация, системное сообщение, ответ

- [x] 5.1 Добавить в `src/config.rs` переменные
  `AGENTD_TASK_STATE_ENABLED` (умолчание `false`),
  `AGENTD_TASK_STATE_AUTO_ENABLED`, `AGENTD_TASK_STATE_MODEL`,
  `AGENTD_TASK_STEP_MAX_CHARS`, `AGENTD_TASK_EXPECTED_ACTION_MAX_CHARS`,
  `AGENTD_TASK_RESUME_BRIEF_MAX_CHARS`; проверка — тесты разбора конфига,
  включая непарсящееся значение и ноль в лимите.
- [x] 5.2 Принять клиентские переключатели `task_state_enabled` и
  `task_state_auto_enabled` в `settings` запроса `POST /v1/chat` так, чтобы
  они сужали операторское умолчание; проверка — тесты: клиент включает
  состояние задачи при выключенном умолчании и выключает трекер при
  включённом умолчании.
- [x] 5.3 Добавить раздел состояния задачи в системное сообщение (через
  список `sections` в `system_message::build`), с пометкой паузы и брифом
  для приостановленной задачи; проверка — тесты на теле запроса в
  `wiremock`: раздел присутствует при включённом состоянии, отсутствует при
  выключенном, после возобновления содержит прежние этап, шаг и ожидаемое
  действие.
- [x] 5.4 Добавить в `ContextDto` (`src/dto.rs`) поля этапа, шага,
  ожидаемого действия, признака паузы и счётчиков применённых и
  отклонённых переходов трекера, опуская их при выключенном состоянии
  задачи; проверка — тесты: поля присутствуют при включённом состоянии и
  отсутствуют при выключенном.

## 6. Сервис: эндпоинты и трекер

- [x] 6.1 Добавить маршруты `GET /v1/chats/{id}/task`,
  `POST /v1/chats/{id}/task/transition`, `POST /v1/chats/{id}/task/pause`,
  `POST /v1/chats/{id}/task/resume` с проверкой владельца и общим конвертом
  ошибок; проверка — тесты: чужой чат недоступен, недопустимый переход даёт
  ошибку с `request_id`, ответ на переход в `done` несёт идентификатор
  новой задачи.
- [x] 6.2 Вернуть журнал переходов активной задачи при чтении состояния
  (этапы, источник, причина, время); проверка — тест: ручной переход даёт
  запись с источником `manual`, журнал новой задачи пуст.
- [x] 6.3 Реализовать фоновый трекер по образцу маршрутизатора памяти:
  один вызов модели после отправки ответа, разбор JSON, валидация автоматом
  и лимитами, результат прогона в `AppState` для блока `context` следующего
  ответа; проверка — тесты: валидное предложение применяется и видно в
  следующем ответе; недопустимое ребро и предложение `done` отбрасываются
  и считаются отклонёнными; на паузе и для запроса без `chat_id` трекер не
  вызывается; отказ трекера не ломает ответ пользователю.
- [x] 6.4 Замаскировать тексты шага, ожидаемого действия, брифа и причины
  в журнале, оставив этапы, источник и длины; проверка — тесты на записи
  журнала при выключенном и включённом `AGENTD_LOG_CONTENT`.

## 7. Клиент: библиотека и TUI (`agent-cli`)

- [x] 7.1 Добавить в `agentclient::chats` тип состояния задачи и методы
  чтения, перехода, паузы и возобновления, а в `ServerAgent` — отправку
  новых полей `settings` и разбор новых полей блока `context`; проверка —
  тесты клиента на `wiremock` с телами запросов и ответов.
- [x] 7.2 Добавить экран состояния задачи по `Ctrl+T` (этап, шаг,
  ожидаемое действие, пауза, последние переходы; переход по допустимым
  рёбрам, правка текстов, пауза и возобновление) и строку состояния задачи
  в шапке чата; проверка — `cargo run -p agentcli -- chat`: экран
  открывается, переход и пауза отражаются в шапке, недопустимые рёбра в
  меню не предлагаются.
- [x] 7.3 Добавить в настройки чата пункты `task_state_enabled` и
  `task_state_auto_enabled` со сбросом на операторское умолчание по
  `Ctrl+D`; проверка — значения сохраняются в файле чата и уходят в
  `settings` запроса (видно в `requests.jsonl`).

## 8. Документация

- [x] 8.1 Обновить `README.md` в `agent-sever`: четыре новых эндпоинта,
  этапы и допустимые переходы, пауза и бриф, переменные
  `AGENTD_TASK_STATE_*`, новые поля блока `context`; проверка — упомянуты
  все переменные из задачи 5.1.
- [x] 8.2 Обновить `README.md` в `agent-cli`: горячая клавиша `Ctrl+T`,
  экран состояния задачи, две новые настройки чата; проверка — список
  горячих клавиш содержит `Ctrl+T`.

## 9. Приёмка

- [ ] 9.1 Прогнать `cargo test` в `agent-cli` и `cargo test` в
  `agent-sever`, затем повторить сценарий из спецификации вручную:
  `planning → execution`, пауза, перезапуск сервиса, возобновление,
  сообщение без пересказа контекста — и убедиться по `requests.jsonl`, что
  системное сообщение несёт прежние этап, шаг и ожидаемое действие.
