## 1. Ядро `agentcore` (`agent-cli`)

- [x] 1.1 Добавить в `crates/core/src/config.rs` поле
  `ChatSettings.profile_id: Option<String>` с `#[serde(default)]` и
  doc-комментарием про операторское умолчание сервиса; проверить
  `cargo test -p agentcore` — сериализация старого файла чата без поля
  читается в `None`.

## 2. Сервис: хранение и встроенные профили (`agent-sever`)

- [x] 2.1 Добавить миграцию `migrations/0005_user_profiles.sql` с таблицей
  `owner_profiles` (`owner`, `id`, `name`, `persona`, `style`, `format`,
  `constraints`, `created_at`, `updated_at`, уникальность `(owner, id)`);
  проверить `cargo test` — существующий тест старта применяет миграции без
  ошибок, а данные прежних таблиц не меняются.
- [x] 2.2 Реализовать в `src/store.rs` операции списка, чтения, создания,
  частичного изменения и удаления профиля в области видимости `owner`;
  проверить тестами хранилища, что профиль владельца X не виден владельцу Y.
- [x] 2.3 Создать `src/profile.rs` со встроенными профилями `teacher`,
  `psychologist`, `reviewer` (константы кода, см. design.md, решение 3) и
  функцией списка «встроенные плюс собственные»; проверить тестом, что
  владелец без собственных профилей получает ровно три встроенных с
  признаком «встроенный».
- [x] 2.4 Добавить в `src/profile.rs` сборку раздела системного сообщения из
  полей профиля (именованные подразделы, пустое поле — нет подраздела) и
  усечение по `AGENTD_PROFILE_MAX_CHARS` с записью события в журнал;
  проверить юнит-тестами состав раздела и факт усечения.

## 3. Сервис: конфигурация и HTTP (`agent-sever`)

- [x] 3.1 Добавить в `src/config.rs` переменные `AGENTD_DEFAULT_PROFILE`
  (умолчание — пусто), `AGENTD_MAX_PROFILES` и `AGENTD_PROFILE_MAX_CHARS`;
  проверить тестами разбора конфигурации умолчания и отказ на некорректном
  числовом значении.
- [x] 3.2 Добавить в `src/dto.rs` DTO профиля (с признаком встроенного) и
  поля `profile_id`/`profile_chars` в `ContextDto` с
  `skip_serializing_if = "Option::is_none"`; проверить тестом сериализации,
  что без профиля полей в ответе нет.
- [x] 3.3 Реализовать в `src/app.rs` обработчики `GET /v1/profiles`,
  `POST /v1/profiles`, `GET /PATCH /DELETE /v1/profiles/{id}` и
  зарегистрировать их в `router` внутри аутентифицируемой ветки `/v1`;
  проверить тестами `src/tests.rs`: созданный профиль виден в списке,
  частичное изменение не затирает прочие поля, удалённый профиль даёт `404`.
- [x] 3.4 Отклонять `400` изменение и удаление встроенного профиля, создание
  профиля без единого непустого поля предпочтений и создание сверх
  `AGENTD_MAX_PROFILES`; проверить тестами на каждый из трёх случаев, что
  ответ — общий конверт ошибки с `request_id`.

## 4. Сервис: подключение профиля к запросу (`agent-sever`)

- [x] 4.1 Добавить в `src/context.rs::assemble` раздел профиля первым — до
  разделов памяти и стратегии (design.md, решение 4) — и заполнение
  `profile_id`/`profile_chars` в `ContextDto`; проверить тестом порядок
  разделов при включённой памяти и стратегии, добавляющей свой раздел.
- [x] 4.2 Валидировать `profile_id` на входе `POST /v1/chat` и на путях
  сохранения настроек чата: несуществующий или чужой профиль — `400` до
  обращения к провайдеру; проверить тестом на `wiremock`, что запрос к
  провайдеру не уходит.
- [x] 4.3 Применять операторское умолчание `AGENTD_DEFAULT_PROFILE`, когда
  `ChatSettings.profile_id` не задан, и собирать запрос без раздела профиля,
  когда пусты оба; проверить тестами обе ветки и совпадение системного
  сообщения с «доprofile» видом во второй.
- [x] 4.4 Ограничить журналирование значений полей профиля флагом
  `AGENTD_LOG_CONTENT`; проверить тестом, что при выключенном флаге в записи
  остаются только `profile_id` и размер раздела.
- [x] 4.5 Добавить в `src/tests.rs` проверку персонализации: один и тот же
  вопрос в трёх чатах с профилями `teacher`, `psychologist`, `reviewer` даёт
  три разных системных сообщения в телах запросов, пойманных `wiremock`.
- [x] 4.6 Добавить в `src/tests.rs` проверку ортогональности: профиль
  подставляется при каждой из четырёх стратегий контекста и одновременно с
  включённой слоистой памятью, не меняя счётчиков стратегии.

## 5. Выкладка ядра и проверка сервиса против `main`

- [x] 5.1 Прогнать `cargo test` в `agent-cli` и запушить изменения ядра в
  `main`; проверить, что ветка `main` содержит поле `profile_id`.
- [x] 5.2 В `agent-sever` временно убрать `[patch]` из
  `.cargo/config.toml`, выполнить `cargo generate-lockfile`, проверить
  `grep -A2 'name = "agentcore"' Cargo.lock` (коммит, а не путь), прогнать
  `cargo test --locked` и вернуть `[patch]` на место.

## 6. Клиент `agent-cli`

- [x] 6.1 Добавить в `crates/client/src/chats.rs` методы списка, чтения и
  создания профилей поверх `/v1/profiles` по образцу методов
  `/memory/long-term`; проверить тестами `crates/client/src/chats/tests.rs`
  на `wiremock` пути и тела запросов.
- [x] 6.2 Добавить подкоманду `profiles` (`list`, `show <id>`,
  `create --file <path>`) в `crates/cli/src/cli.rs` и её выполнение в
  `crates/cli/src/chats.rs`; проверить запуском
  `cargo run -p agentcli -- profiles list` против локального сервиса.
- [x] 6.3 Добавить в `crates/cli/src/tui.rs` поле выбора профиля в раздел
  «Контекст» параметров чата (`Ctrl+P`) со списком, загруженным с сервиса, и
  подсказкой; проверить, что выбранный профиль сохраняется в настройках чата
  и переживает перезапуск клиента.

## 7. Документация

- [x] 7.1 Обновить `agent-sever/README.md`: сущность профиля, три встроенных
  профиля, эндпоинты `/v1/profiles`, переменные `AGENTD_DEFAULT_PROFILE`,
  `AGENTD_MAX_PROFILES`, `AGENTD_PROFILE_MAX_CHARS`, порядок разделов
  системного сообщения и поля `profile_id`/`profile_chars` блока `context`;
  проверить, что перечислённые переменные совпадают с `src/config.rs`.
- [x] 7.2 Обновить `agent-cli/README.md`: выбор профиля в параметрах чата,
  команды `profiles`, поле `profile_id` в настройках чата; проверить, что
  описанные команды совпадают с `crates/cli/src/cli.rs`.
